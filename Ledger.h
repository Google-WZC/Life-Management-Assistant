#pragma once

#include <stdio.h>  // This can be removed, because other head-file(such as LedgerFunc.h) already includes it.
#include <stdlib.h> // for system("clear")  This can be removed, because other head-file(such as LedgerFunc.h) already includes it.
#include "LedgerStruc.h"
#include "LedgerFunc.h"
#include "UI.h"